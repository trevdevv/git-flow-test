name: Tests

on:
  push:
    branches: 
      - develop 
  pull_request:
    branches: [ develop ]

jobs:
  UnitTest:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Run Jest
      uses: stefanoeb/jest-action@1.0.2